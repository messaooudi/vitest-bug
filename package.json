{
  "name": "@ome/monorepo-for-test",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {},
  "devDependencies": {
    "jsdom": "^22.0.0",
    "lerna": "^6.6.2",
    "typescript": "^4.8.4",
    "vitest": "^0.31.0"
  },
  "stackblitz": {
    "startCommand": "yarn tsc -b && yarn vitest run"
  }
}
